mirascope>=2.0.0
requests>=2.28.0
beautifulsoup4>=4.11.0
PyYAML>=6.0
tiktoken>=0.5.0
Pillow>=10.0.0
mss>=9.0.0
python-dotenv>=1.0.0

# Voice output dependencies (optional - only needed for /voice mode)
sounddevice>=0.4.6           # Speaker output (requires libportaudio2 + libasound2-plugins on WSL)
numpy>=1.24                  # Audio array manipulation
pocket-tts>=0.1              # Kyutai Pocket TTS - local, CPU-only (recommended)
pyttsx3>=2.90              # Fallback local TTS (lower quality, no model download)
openai>=1.0                # OpenAI TTS API (cloud alternative)
